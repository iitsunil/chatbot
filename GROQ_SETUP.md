# Groq API Setup (FREE)

Your chatbot has been updated to use **Groq** - a completely FREE and FAST AI service!

## Get Your Free API Key

1. Go to https://console.groq.com/
2. Sign up for a free account (no credit card required)
3. Go to API Keys section
4. Create a new API key

## Setup

1. Create or update your `.env.local` file in the root directory:

```bash
GROQ_API_KEY=your_groq_api_key_here
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_key
```

2. Restart your development server:

```bash
npm run dev
```

## What Changed

- ✅ Switched from Google Gemini to Groq (100% FREE)
- ✅ Using `llama-3.1-70b-versatile` model (very powerful and fast)
- ✅ No credit card required
- ✅ Generous free tier limits
- ✅ Much faster response times

## Features

- Free tier includes up to 14,400 requests per day
- Very fast inference (faster than most paid APIs)
- High-quality responses using Meta's Llama 3.1 model

Your chatbot will now work without any API costs! 🎉
